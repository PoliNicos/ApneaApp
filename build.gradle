buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Updated to 8.2.2 for better compatibility
        classpath 'com.android.tools.build:gradle:8.2.2'
        // Updated to 1.9.20 to fix the "HasConvention" error
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.20"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
